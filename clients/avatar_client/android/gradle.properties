org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Flutter plugins are cached on C: while this project is on F:, which breaks
# Kotlin incremental cache path relativization on Windows.
kotlin.incremental=false
# Properties expected by the official Cubism Framework module build script.
PROP_COMPILE_SDK_VERSION=36
PROP_MIN_SDK_VERSION=23
PROP_TARGET_SDK_VERSION=36
